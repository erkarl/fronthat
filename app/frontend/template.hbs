{{#job-list-container as |jobs fetching|
}}
  {{filtered-jobs-list
    filter='frontend'
    jobs=jobs
    search=search
    fetching=fetching
    scrollPosition=scrollPosition
    firstVisibleChanged=(action 'firstVisibleChanged')
    filterBySearch=(route-action 'filterBySearch')
  }}
{{/job-list-container}}
